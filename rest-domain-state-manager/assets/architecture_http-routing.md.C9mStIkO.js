import{_ as a,o as n,c as i,a5 as e}from"./chunks/framework.QFIuQCaY.js";const k=JSON.parse('{"title":"HTTP 자동 라우팅","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/http-routing.md","filePath":"architecture/http-routing.md"}'),t={name:"architecture/http-routing.md"};function p(l,s,d,r,h,o){return n(),i("div",null,[...s[0]||(s[0]=[e(`<h1 id="http-자동-라우팅" tabindex="-1">HTTP 자동 라우팅 <a class="header-anchor" href="#http-자동-라우팅" aria-label="Permalink to &quot;HTTP 자동 라우팅&quot;">​</a></h1><p>이 문서는 <code>save()</code> 가 HTTP 메서드를 자동으로 결정하는 알고리즘의 설계 배경, 구현 원리, 그리고 엣지 케이스 처리 방식을 기술합니다.</p><h2 id="설계-배경" tabindex="-1">설계 배경 <a class="header-anchor" href="#설계-배경" aria-label="Permalink to &quot;설계 배경&quot;">​</a></h2><p>REST API에서 HTTP 메서드는 단순한 액션 식별자가 아닙니다. 각 메서드는 서버-클라이언트 간 약속된 의미론(Semantics)과 멱등성(Idempotency) 규약을 내포합니다.</p><ul><li><strong>POST</strong> (비멱등) — 리소스를 새로 생성합니다. 동일한 요청을 두 번 보내면 두 개의 리소스가 생성됩니다.</li><li><strong>PUT</strong> (멱등) — 타겟 리소스를 완전히 교체합니다. 동일한 요청을 N번 보내도 결과는 항상 동일합니다. 요청 본문에 리소스의 <strong>모든 필드</strong>가 포함되어야 합니다.</li><li><strong>PATCH</strong> (부분/비멱등) — 리소스의 일부만 변경합니다. <a href="https://datatracker.ietf.org/doc/html/rfc6902" target="_blank" rel="noreferrer">RFC 6902 JSON Patch</a> 포맷을 사용합니다.</li></ul><p>이 의미론을 준수하지 않으면 데이터 무결성 오류가 발생합니다. 예를 들어 변경된 필드만 있다는 이유로 항상 PATCH를 보내다가 요청 실패 시 서버 상태와 불일치가 생깁니다.</p><h2 id="dirty-checking-—-설계-원리" tabindex="-1">Dirty Checking — 설계 원리 <a class="header-anchor" href="#dirty-checking-—-설계-원리" aria-label="Permalink to &quot;Dirty Checking — 설계 원리&quot;">​</a></h2><p>Java Hibernate 등의 ORM에서 오래전부터 사용해온 <strong>Dirty Checking</strong> 메커니즘을 JavaScript Proxy 기반으로 구현합니다.</p><p>핵심 아이디어는 단순합니다: <em>어떤 최상위 필드가 변경되었는가</em>를 Set으로 추적하여, 변경된 필드의 비율에 따라 전체 교체(PUT) 또는 부분 교체(PATCH)를 결정합니다.</p><h3 id="changelog-vs-dirtyfields-—-역할-분리" tabindex="-1">changeLog vs dirtyFields — 역할 분리 <a class="header-anchor" href="#changelog-vs-dirtyfields-—-역할-분리" aria-label="Permalink to &quot;changeLog vs dirtyFields — 역할 분리&quot;">​</a></h3><p>두 자료구조는 서로 다른 목적을 가집니다.</p><table class="param-table"><thead><tr><th>구분</th><th>changeLog</th><th>dirtyFields</th></tr></thead><tbody><tr><td>자료구조</td><td><code>ChangeLogEntry[]</code></td><td><code>Set&lt;string&gt;</code></td></tr><tr><td>저장 단위</td><td>RFC 6902 op 단위 (ADD/REPLACE/REMOVE)</td><td>변경된 최상위 키</td></tr><tr><td>목적</td><td>PATCH 페이로드 직렬화</td><td>PUT/PATCH 분기 비율 계산</td></tr><tr><td>중복 처리</td><td>동일 경로 변경 시 항목이 쌓임</td><td>Set이므로 동일 키 중복 없음</td></tr><tr><td>질문</td><td>&quot;어떻게 바뀌었나 (감사 이력)&quot;</td><td>&quot;어느 키가 바뀌었나 (존재 여부)&quot;</td></tr></tbody></table><h3 id="최상위-키-추출-원리" tabindex="-1">최상위 키 추출 원리 <a class="header-anchor" href="#최상위-키-추출-원리" aria-label="Permalink to &quot;최상위 키 추출 원리&quot;">​</a></h3><p>모든 변경 경로는 JSON Pointer(RFC 6901) 스타일로 <code>/</code> 로 시작합니다. <code>dirtyFields</code> 는 경로의 두 번째 세그먼트(최상위 키)만 추출합니다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;/name&#39;              → split(&#39;/&#39;)[1] → &#39;name&#39;</span></span>
<span class="line"><span>&#39;/address/city&#39;      → split(&#39;/&#39;)[1] → &#39;address&#39;   (중첩 변경은 최상위만)</span></span>
<span class="line"><span>&#39;/items/0/price&#39;     → split(&#39;/&#39;)[1] → &#39;items&#39;</span></span>
<span class="line"><span>&#39;/items&#39;             → split(&#39;/&#39;)[1] → &#39;items&#39;     (배열 전체 REPLACE도 동일)</span></span></code></pre></div><p>이 설계는 <code>totalFields = Object.keys(target).length</code> 와 수학적으로 정합합니다. <code>address.city</code> 를 변경한 것은 루트 객체 기준으로 <code>address</code> 하나를 변경한 것이며, PUT/PATCH 비율 계산은 루트 필드 수를 기준으로 합니다.</p><h2 id="분기-알고리즘" tabindex="-1">분기 알고리즘 <a class="header-anchor" href="#분기-알고리즘" aria-label="Permalink to &quot;분기 알고리즘&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>save() 진입</span></span>
<span class="line"><span> │</span></span>
<span class="line"><span> ├─ _isNew === true</span></span>
<span class="line"><span> │    └─ POST</span></span>
<span class="line"><span> │         payload: JSON.stringify(getTarget())</span></span>
<span class="line"><span> │         성공 시: _isNew = false</span></span>
<span class="line"><span> │</span></span>
<span class="line"><span> └─ _isNew === false</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      dirtyRatio = dirtyFields.size / Object.keys(getTarget()).length</span></span>
<span class="line"><span>      (totalFields === 0인 경우 dirtyRatio = 0으로 처리 — ZeroDivisionError 방어)</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ├─ dirtyFields.size === 0</span></span>
<span class="line"><span>      │    └─ PUT</span></span>
<span class="line"><span>      │         payload: JSON.stringify(getTarget())</span></span>
<span class="line"><span>      │         의미: 변경 없는 의도적 재저장 — 멱등성 보장</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ├─ dirtyRatio &gt;= DIRTY_THRESHOLD (0.7)</span></span>
<span class="line"><span>      │    └─ PUT</span></span>
<span class="line"><span>      │         payload: JSON.stringify(getTarget())</span></span>
<span class="line"><span>      │         의미: 70% 이상 변경 — 전체 교체가 PATCH 오버헤드보다 효율적</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      └─ dirtyRatio &lt; DIRTY_THRESHOLD</span></span>
<span class="line"><span>           └─ PATCH</span></span>
<span class="line"><span>                payload: toPatch(changeLog)  → RFC 6902 배열</span></span></code></pre></div><h3 id="dirty-threshold-0-7-의-선택-근거" tabindex="-1">DIRTY_THRESHOLD = 0.7 의 선택 근거 <a class="header-anchor" href="#dirty-threshold-0-7-의-선택-근거" aria-label="Permalink to &quot;DIRTY_THRESHOLD = 0.7 의 선택 근거&quot;">​</a></h3><p>PATCH 방식은 변경된 필드만 전송하여 페이로드 크기와 서버 처리 비용을 줄입니다. 그러나 필드 수가 많을수록 JSON Patch 배열 자체의 직렬화 오버헤드가 증가합니다. 전체 필드의 70% 이상이 변경된 경우, PUT으로 전체 객체를 한 번에 전송하는 것이 구조적으로 더 단순하고 효율적이라는 판단입니다. 이 임계값은 <code>src/constants/dirty.const.js</code> 에 상수로 선언되어 있으며 필요 시 조정 가능합니다.</p><h2 id="optimistic-update-롤백" tabindex="-1">Optimistic Update 롤백 <a class="header-anchor" href="#optimistic-update-롤백" aria-label="Permalink to &quot;Optimistic Update 롤백&quot;">​</a></h2><p><code>save()</code> 는 요청을 보내기 전에 현재 상태의 스냅샷을 생성합니다. 스냅샷은 <code>structuredClone()</code> 을 사용한 깊은 복사이므로, 이후 <code>domainObject</code> 가 변경되어도 스냅샷은 영향받지 않습니다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>save() 진입</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  snapshot = {</span></span>
<span class="line"><span>    data:        structuredClone(getTarget()),  ← 깊은 복사</span></span>
<span class="line"><span>    changeLog:   getChangeLog(),                ← 얕은 복사본 (이미 [...changeLog])</span></span>
<span class="line"><span>    dirtyFields: getDirtyFields(),              ← new Set 복사본</span></span>
<span class="line"><span>    isNew:       _isNew,                        ← 원시값</span></span>
<span class="line"><span>  }</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  try:</span></span>
<span class="line"><span>    _fetch() 호출</span></span>
<span class="line"><span>    성공 → clearChangeLog(), clearDirtyFields(), broadcast</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  catch:</span></span>
<span class="line"><span>    _rollback(snapshot)</span></span>
<span class="line"><span>      ├─ restoreTarget(snapshot.data)      ← Proxy 우회 직접 복원</span></span>
<span class="line"><span>      ├─ restoreChangeLog(snapshot.changeLog)</span></span>
<span class="line"><span>      ├─ restoreDirtyFields(snapshot.dirtyFields)</span></span>
<span class="line"><span>      ├─ _isNew = snapshot.isNew</span></span>
<span class="line"><span>      └─ if debug: broadcast()</span></span>
<span class="line"><span>    throw err                              ← 반드시 re-throw</span></span></code></pre></div><h3 id="restoretarget-—-proxy-우회-복원" tabindex="-1">restoreTarget — Proxy 우회 복원 <a class="header-anchor" href="#restoretarget-—-proxy-우회-복원" aria-label="Permalink to &quot;restoreTarget — Proxy 우회 복원&quot;">​</a></h3><p>롤백 시 <code>domainObject</code> 의 프로퍼티를 복원하는 작업은 Proxy 트랩을 우회하여 원본 객체에 직접 접근합니다. 이렇게 하지 않으면 복원 작업 자체가 <code>changeLog</code> 에 기록되어 이력이 오염됩니다.</p><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// createProxy() 클로저 내부</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">restoreTarget</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Array.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isArray</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(domainObject)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        domainObject.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">splice</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 배열 루트인 경우 splice로 처리</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        domainObject.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data)       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// (length 복원 보장)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Object.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">keys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(domainObject)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            Reflect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">deleteProperty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(domainObject, key)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 기존 키 제거</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Object.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assign</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(domainObject, data)              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 스냅샷으로 채우기</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>이 작업은 Proxy 트랩이 아닌 클로저 내부의 <code>domainObject</code> 참조에 직접 접근하므로, 트랩이 개입하지 않습니다.</p><h2 id="엣지-케이스-처리" tabindex="-1">엣지 케이스 처리 <a class="header-anchor" href="#엣지-케이스-처리" aria-label="Permalink to &quot;엣지 케이스 처리&quot;">​</a></h2><blockquote><p><strong>빈 객체 (<code>totalFields === 0</code>)</strong></p></blockquote><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>dirtyRatio = 0   → dirtyFields.size === 0 조건 우선 → PUT</span></span></code></pre></div><blockquote><p><strong>배열 루트 객체에서의 save()</strong></p></blockquote><p>배열 루트 객체는 <code>Object.keys()</code> 가 인덱스 문자열을 반환합니다. <code>totalFields</code> 계산 자체는 동일하게 동작하지만, 배열 데이터를 가진 인스턴스에서 <code>save()</code> 를 호출하는 경우는 <code>DomainRenderer</code> 의 렌더링 소스로만 사용하는 것이 일반적입니다.</p><blockquote><p><strong>POST 실패 후 isNew 복원</strong></p></blockquote><p><code>_fetch()</code> 가 throw하면 <code>this._isNew = false</code> 줄이 실행되지 않습니다. 그러나 스냅샷에 <code>isNew: true</code> 가 포함되어 있으므로 <code>_rollback()</code> 이 항상 일관되게 복원합니다.</p>`,34)])])}const g=a(t,[["render",p]]);export{k as __pageData,g as default};
