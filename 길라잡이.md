# 길라잡이

1. 다크 사이드바 + 화이트 컨텐츠 -- React Admin 스타일 그대로. `#111827` 사이드바에 파란 active 라인.
2. 코드 블록 -- highlight.js 다크 테마 + 언어 라벨 + Copy 버튼 자동 주입 (`site.js`)
3. Intersection Observer -- 스크롤 위치에 따라 사이드바 active 항목 자동전환.
4. 새 프로젝트 추가 방법 -- `lab/` 하위 디렉토리 추가 후 `index.html` 파일 push. 그 다음, **Hub `index.html`**의 `.project-grid` 에 새 카드 추가.
